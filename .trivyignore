# Ignore the intentional bad deployment file so it doesn't break the build
tests/bad-deployment.yaml

# Ignore ReadOnlyRootFilesystem rule because standard Nginx needs to write to /var/cache
# We accept this risk for this demo project.
AVD-KSV-0014
